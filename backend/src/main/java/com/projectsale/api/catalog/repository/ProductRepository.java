package com.projectsale.api.catalog.repository;
import com.projectsale.api.catalog.entity.Product; import com.projectsale.api.user.entity.EntityStatus; import java.util.*; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.*; import org.springframework.data.repository.query.Param;
public interface ProductRepository extends JpaRepository<Product,Long>,JpaSpecificationExecutor<Product>{Optional<Product> findBySlugAndStatusAndDeletedAtIsNull(String slug,EntityStatus status);List<Product> findTop4ByCategoryIdAndIdNotAndStatusAndDeletedAtIsNullOrderByRatingAvgDescCreatedAtDescIdDesc(Long categoryId,Long id,EntityStatus status);@Query(value="select p.* from products p where p.status='ACTIVE' and p.deleted_at is null and to_tsvector('simple',p.name || ' ' || coalesce(p.description,'')) @@ websearch_to_tsquery('simple',:q)",countQuery="select count(*) from products p where p.status='ACTIVE' and p.deleted_at is null and to_tsvector('simple',p.name || ' ' || coalesce(p.description,'')) @@ websearch_to_tsquery('simple',:q)",nativeQuery=true) Page<Product> searchVisible(@Param("q")String q,Pageable pageable);}
