package com.projectsale.api.auth.security.dto;

/** Coarse, display-only device platform classification. */
public enum DevicePlatform {
  IOS,
  ANDROID,
  WINDOWS,
  MACOS,
  LINUX,
  OTHER,
  UNKNOWN
}
